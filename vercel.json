{
  "version": 2,
  "builds": [{ "src": "index.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/assets/(.*)", "dest": "assets" },
    { "src": "/views/(.*)", "dest": "views" },
    { "src": "/routing/(.*)", "dest": "routing" },
    { "src": "/(.*)", "dest": "index.js" }
  ]
}
